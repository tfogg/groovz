
	

	<%-# link_to "Follow", new_user_followers_path(follow_id: @user) %>



<% if @user.is_artist? %>
	<%= render "artist" %>
<%end%>

<% if @user.is_venue? %>
	<%= render "venue" %>
<%end%>

<% if @user.is_fan? %>
	<%= render "fan" %>
<%end%>
